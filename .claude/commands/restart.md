Stop and restart the Flask development server.

Steps:
1. Kill any process running on port 5001
2. Start the Flask server using `python3 backend/app.py` in the background
3. Wait 2 seconds and check the output logs to verify successful startup
4. Report the server status and access URL (http://localhost:5001)
